#include <stdio.h>

/**
 * main - the strating point
 *
 * for - to loop through the numbers
 *
 * printf - to outpot the result
 *
 * Return: the return value
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");

	return (0);

}
